# Setup the project

First go inside the project directory (i.e. cwaf-ontology).
Then, to compile the project use 
```
mvn compile
```
 then run the desired class with
 ```
 java -cp target/classes <your class name>
 ```

 ---
 > tested with Java 21.0.3
